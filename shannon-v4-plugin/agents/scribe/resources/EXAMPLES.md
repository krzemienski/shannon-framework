# SCRIBE Agent - Examples

> Loaded on-demand (Tier 3)

## Examples

### cURL Example
```bash
curl -X GET \
  'https://api.example.com/v1/resource?filter=active' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```